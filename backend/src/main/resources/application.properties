spring.application.name=backend

# Database Configuration (Database ??????)
# MySQL database ??? connection details
spring.datasource.url=jdbc:mysql://localhost:3306/corehive_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# Database tables auto create/update ?????
spring.jpa.hibernate.ddl-auto=update

# SQL queries console ??? ???????? (development ????)
spring.jpa.show-sql=true

# SQL queries formatted ?????? ????????
spring.jpa.properties.hibernate.format_sql=true

# Database dialect (MySQL specific features)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration (JWT Token ??????)
# Token sign ????? secret key ???? (production ??? secure key ???? use ?????)
jwt.secret=myVeryLongAndSecureSecretKeyForJWTTokenGenerationAndValidation2024CoreHive

# Token ????? ?????? valid (24 hours = 86400000 milliseconds)
jwt.expiration=86400000

# Server Configuration
# Backend server port (React development server 3000 use ?????, backend 8080)
server.port=8080

# CORS Configuration (Cross-Origin Resource Sharing)
# React app (http://localhost:3000) ? API calls ????? allow ?????
cors.allowed-origins=http://localhost:3000

# Logging Configuration
# Application logs level
logging.level.com.corehive.backend=DEBUG
logging.level.org.springframework.security=DEBUG

# Actuator Configuration
# Health check endpoint enable ?????
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Mail service configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shehangarusinghe@gmail.com
spring.mail.password=vhsx sfux vbrk rgon
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.devtools.restart.enabled=false

# File Upload Configuration
file.upload.dir=backend/uploads/business-registrations
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB
